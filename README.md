# Dhruv Mehta Portfolio

A premium one-page portfolio for Dhruv Mehta, positioned around business continuity, information security, and business analysis, with machine learning as technical depth.

## Stack
- Vite
- React 19
- TypeScript
- Tailwind CSS v4
- Framer Motion

## Local development
1. Install dependencies with `npm install`
2. Start the dev server with `npm run dev`
3. Create a production build with `npm run build`
4. Run lint with `npm run lint`

## Deploy
This repository is configured for GitHub Pages project-site deployment.

- Vite base path: `/Resume/`
- Build output: `dist/`
- Workflow: `.github/workflows/deploy.yml`

Pushes to `main` trigger the Pages workflow. If the repository has not been configured to publish from GitHub Actions yet, a repo admin may need to enable GitHub Pages with GitHub Actions as the source.

## Content provenance
- Primary source: the provided original resume and redesigned CV
- Public downloadable CV: `public/cv/dhruv-mehta-cv.pdf`
- Stitch export reference: `stitch-export/4874795684782609962/`

## Notes
- The public page uses only verified facts from the supplied materials.
- On-page contact stays limited to email and LinkedIn.
- Phone remains in the downloadable CV.
